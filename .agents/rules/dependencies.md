# Rule: Dependency Management

This rule governs adding external packages or modifying dependency files.

## Adding Dependencies
- **Ask Before Adding**: Never add new dependencies (gems) to `passcraft.gemspec` or `Gemfile` without explicit user permission.
